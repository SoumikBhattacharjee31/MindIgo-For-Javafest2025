import React from "react";

const SubmitButton = () => {
  return (
    <button
      type="submit"
      className="btn btn-primary w-full px-4 py-2 text-white bg-blue-500 hover:bg-blue-700"
    >
      Send Reset Link
    </button>
  );
};

export default SubmitButton;
